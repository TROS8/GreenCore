import requests

BASE_URL = "http://localhost:8080{{ base_url }}"


def test_get_all_{{ class_name.lower() }}s():
    response = requests.get(f"{BASE_URL}/{{ table_name }}")
    assert response.status_code == 200
    assert isinstance(response.json(), list)
